[default]
logging_config = logging.ini
host = 0.0.0.0
port = 8888
workers = 1
reload = true
cors_allowed_origins = http://localhost:3333
base_url = https://localhost:8888

[db]
db_url = postgresql+psycopg2://neo:trinity@localhost:5678/morpheus_db

[integrations]
; API keys can be set here for local development
; They will be used if not found in environment variables or AWS Secrets Manager
; Uncomment and replace with actual keys for local testing
; CodexApiKey = "your-codex-api-key"
; ElfaApiKey = "your-elfa-api-key"
; GeminiApiKey = "your-gemini-api-key"
; OpenaiApiKey = "your-openai-api-key"
; ApifyApiKey = "your-apify-api-key"
; BraveApiKey = "your-brave-api-key"
